# Hero video

Subir aquí el video maestro de 10 segundos con este nombre exacto:

`andres-hero-10s.mp4`

La implementación usa el mismo archivo para:
- Desktop: recorte dentro del marco actual del hero, sin alterar layout.
- Mobile: video vertical a pantalla completa con el copy superpuesto y scroll normal.

No reemplazar ni borrar las imágenes actuales; funcionarán como fallback/poster.
